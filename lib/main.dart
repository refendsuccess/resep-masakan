import 'package:flutter/material.dart';

import 'package:resep_makanan_sederhana/main_page.dart';

void main() => runApp(Makanan2App());

class Makanan2App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: MainScreen(),
    );
  }
}
